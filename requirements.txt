# Dependencies for the Gradio web app (app.py).
# The upstream desktop GUI's requirements are preserved in requirements-desktop.txt.

gradio>=6.0
gradio_image_annotation>=0.5
huggingface_hub>=0.30

# Plain CPU onnxruntime: the detection/OCR sessions reported CPUExecutionProvider
# even inside a ZeroGPU allocation, so the much larger onnxruntime-gpu bought
# nothing. Only translation (torch) actually uses the device.
onnxruntime>=1.17
opencv-python-headless>=4.9
numpy>=1.26
scipy>=1.11
pyctcdecode>=0.5
thin-plate-spline>=1.0

python-docx>=1.1
reportlab>=4.0

# Tibetan -> English translation (billingsmoore/mlotsawa-ground-base).
transformers>=4.40
torch>=2.2
sentencepiece

# pyewts is deliberately absent: its sdist imports pkg_resources at build time,
# which setuptools 81+ removed, so pip cannot build it in a current environment.
# It is pure Python and Apache-2.0, so it is vendored under vendor/.
# See THIRD_PARTY_NOTICES.md.
